# pcp-messages

Protobuf definitions for PCP (Personal Custody Package) messages.

- `proto/`   — protobuf source files
- `buf.yaml` — Buf module config (lint + breaking-change rules)
- `rust/`    — Rust crate `orb-pcp-messages`
- `go/`      — Go bindings (placeholder, to be set up)
