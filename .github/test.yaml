name: Safe Test Workflow

on:
  push:
    branches:
      - test-branch

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run test command
        run: |
          echo "Hello from safe workflow!"
          whoami
