[package]
name = "orb-schema"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
prost-build = "0.10.1"

[dependencies]
prost = "0.10.1"

[dev-dependencies]
bytes = "1.1.0"
